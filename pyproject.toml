[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Big-Data-Finance"
authors = [{name = "Xhcao", email = "caoxianghan@gmail.com"}]
dynamic = ["version", "description"]